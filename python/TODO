* refactor command line processing to use data.py
* Generate new certificates
    - SCEP (NDES) 
    - ADCS using ACME proxy
* auto-generate certificate passphrase on export (thanks dan!)
* data model for storing certificate information
* store certificate information
* certificate selector  
* j2 template for csr generation
* clone certificate

